

Measure distance across peak in GoogleMaps
Make sure distance is at least 2 miles, with peak centered

Get start and end lat/long of distance line.

Plug into here with 80 points requested:

http://localhost/jcr13/plotElevation/getElevationSVG.php

Import SVG into Inkscape layout.

Align pink rectangle with bottom horizontal guide

Resize peak and pink rectangle to 20mm tall (lock aspect ratio)

Center peak in circle

Ungroup and delete pink rectangle.

Make sure peak is top layer (above inner circle).

Select both peak line and inner circle.

Use "Division" operation to make cut-outs.

Change line color on upper part of cut-out to green

Select all text and do Object To Path

